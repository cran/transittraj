% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs_helpers.R
\name{filter_by_route}
\alias{filter_by_route}
\title{Filter GTFS to a desired route(s) and direction(s)}
\usage{
filter_by_route(gtfs, route_ids, dir_id = NULL)
}
\arguments{
\item{gtfs}{A \code{tidygtfs} object.}

\item{route_ids}{A vector containing the desired route ID(s).}

\item{dir_id}{Optional. A vector containing the desired direction ID(s).}
}
\value{
A tidygtfs object containing only information relevant to the desired
route and direction.
}
\description{
This function returns a new \code{tidygtfs} object with only the information
relevant to your desired routes and directions. All fields included in the
input \code{gtfs} will be filtered. See \code{Details} for more information about
required files and fields
}
\details{
The following files and fields are required for this function:
\itemize{
\item \code{routes}: with \code{route_id} and \code{agency_id}
\item \code{agency}: with \code{agency_id}
\item \code{trips}: with \code{route_id}, \code{direction_id}, \code{shape_id}, \code{service_id}, and
\code{trip_id}
\item \code{stop_times}: with \code{stop_id} and \code{trip_id}
}

The following files are optional. If included, they must include
the listed fields:
\itemize{
\item \code{stops}: with \code{stop_id}
\item \code{shapes}: with \code{shape_id}
\item \code{calendar}: with \code{service_id}
\item \code{calendar_dates}: with \code{service_id}
\item \code{transfers}: with \code{trip_id} and \code{stop_id}
\item \code{frequencies}: with \code{trip_id}
\item \code{fare_rules}: with \code{route_id}
\item \code{feed_info}
}

For these optional files, the function will detect whether they are present.
If so, they will be filtered; if not, they will be left \code{NULL} in the new
GTFS. If any required file or field is missing, an error will be thrown
describing what is missing.
}
\examples{
# Set my parameters
my_route <- "804"
my_dir <- 0

# Filter WMATA GTFS
lineE_gtfs <- filter_by_route(gtfs = lacmta_gtfs,
                              route_ids = my_route,
                              dir_id = my_dir)
summary(lineE_gtfs)
}
