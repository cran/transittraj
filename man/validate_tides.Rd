% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tides_validation.R
\name{validate_tides}
\alias{validate_tides}
\title{Check if AVL data meets TIDES standards}
\usage{
validate_tides(avl_df)
}
\arguments{
\item{avl_df}{A dataframe of AVL data, either in GPS (longitude/latitude)
form or linearized distance form.}
}
\value{
A dataframe of each required field, required data type, whether
the field is present, and whether the data type matches expectations.
}
\description{
The transit integrated data exchange standard (TIDES) specifies columns that
should be present in AVL data tables and the data types of these columns.
This function verifies if those columns are present in the input \code{avl_df}
are of the correct data type. See \code{Details} for more information.
}
\details{
The AVL cleaning functions in this package generally require the input
dataframes to adhere to the TIDES \code{vehicle_locations}
\href{https://tides-transit.org/main/tables/#vehicle-locations}{table schema}. The
following columns and data types are checked by this validator:
\itemize{
\item \code{location_ping_id}: Should be a \code{character} string.
\item \code{trip_id_performed}: Should be a \code{character} string.
\item \code{event_timestamp}: Should be a \code{POSIXct} date-time.
\item \code{vehicle_id}: Should be a \code{character} string.
\item \code{operator_id}: Should be a \code{character} string. This field is not standard
in TIDES \code{vehicle_locations}, and is not a strict requirement for any AVL
processing functions.
\item \code{longitude} and \code{latitude}: Should be \code{numeric}. These fields are required
only to linearize AVL data, and not used afterwards.
\item \code{distance}: Should be \code{numeric}. This field is not standard in TIDES
\code{vehicle_locations}, and is generated by \code{get_linear_distances()}. It is
required by most other AVL processing functions.
\item \code{speed}: Should be \code{numeric}. This field is not a strict requirement for
any AVL processing functions.
}

Each AVL processing function in \code{transittraj} uses specific fields. Each
function verifies that the required fields and data types are present before
proceeding.
}
\examples{
lacmta_val <- validate_tides(avl_df = lacmta_avl)
print(lacmta_val)
}
