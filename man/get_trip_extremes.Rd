% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory_prediction.R
\name{get_trip_extremes}
\alias{get_trip_extremes}
\title{Get the distance and time range of each trip in a trajectory object}
\usage{
get_trip_extremes(trajectory, filter_trips = NULL)
}
\arguments{
\item{trajectory}{A trajectory object.}

\item{filter_trips}{Optional. A vector of \code{trip_id_performed}s to filter the
dataframe to. At least one must of \code{filter_trips} must be present in
\code{trajectory}. Default is \code{NULL}, where all \code{trip_id_performed}s in
\code{trajectory} are returned.}
}
\value{
A dataframe with the columns \code{trip_id_performed}, \code{min_time},
\code{max_time}, \code{min_dist}, and \code{max_dist}.
}
\description{
This function extracts the time and distance ranges stored in a trajectory
object and formats them into a dataframe. The dataframe can
be filtered to a desired set of \code{trip_id_performed}s.
}
\examples{
# Get input data
lineE_traj <- new_transittraj_data("get_trajectory_fun")

# Run function
lineE_extremes <- get_trip_extremes(lineE_traj)
print(lineE_extremes)
}
