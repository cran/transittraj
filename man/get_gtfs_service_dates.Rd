% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs_helpers.R
\name{get_gtfs_service_dates}
\alias{get_gtfs_service_dates}
\title{Get a dataframe of all service dates and their service IDs from a GTFS}
\usage{
get_gtfs_service_dates(
  gtfs,
  date_min = NULL,
  date_max = NULL,
  use_calendar_table = "calendar"
)
}
\arguments{
\item{gtfs}{A tidygtfs object.}

\item{date_min}{Optional. The starting (earliest possible) \code{Date} object for
the returned dataframe. Default is \code{NULL}, where the earliest date in the
GTFS will be used.}

\item{date_max}{Optional. The ending (latest possible) \code{Date} object for the
returned dataframe. Default is \code{NULL}, where the latest date in the GTFS
will be used.}

\item{use_calendar_table}{Optional. Should the GTFS's \code{calendar.txt} or
\code{calendar_dates.txt} be used for the feasible date range? Must be
\code{"calendar"} or \code{"calendar_dates"}. Default is \code{"calendar"}.}
}
\value{
A dataframe with \code{Date} column \code{date} and character column
\code{service_id}.
}
\description{
This function returns a dataframe with each date covered by a GTFS and the
\code{service_id} run on that date. This data is extracted from the \code{calendar.txt}
and \code{calendar_dates.txt} files, depending on how the GTFS is structured. See
\code{Details} for a discussion.
}
\details{
The GTFS standard allows for two different structurings of \code{calendar.txt}
and \code{calendar_dates.txt}:
\itemize{
\item Standard service in \code{calendar.txt}, with exceptions in
\code{calendar_dates.txt}. Here, \code{calendar.txt} will list the standard service ID
by weekday (e.g., Monday, Tuesday, etc.), and \code{calendar_dates.txt} lists
specific dates which are exceptions to this. In this scenario,
\code{get_gtfs_service_dates()} will get enumerate all weekdays and dates
in \code{calendar.txt}, and assign the correct \code{service_id} to it, depending on
if the date is listed as an exception in \code{calendar_dates.txt}.
\item All dates of service are enumerated in \code{calendar_dates.txt}, and
\code{calendar.txt} is not used. In this scenario, \code{get_gtfs_service_dates()}
will simply filter, clean, and return this table.
}

Use the input parameter \code{use_calendar_table} to control which method to use.
If \code{use_calendar_table = "calendar"}, the former method will be used; if
\code{use_calendar_table = "calendar_dates"}, the latter will be used. To
restrict the date enumeration to only a specific window, set \code{date_min}
and \code{date_max}.

This function is also intended for GTFS feeds with only one service ID per
day. Some GTFS providers (including \code{lacmta_gtfs}) have unique \code{service_id}s
by route, and thus service dates do not have unique \code{service_id}s.
Consider filtering your GTFS to a single route before using this function
(see \code{filter_by_route()}). If there are multiple service IDs on a given
day, the first appearing will be returned.
}
\examples{
# Set parameters
study_date <- as.Date("2026-05-27")

# Get needed input data
lineE_gtfs <- filter_by_route(gtfs = lacmta_gtfs, route_ids = "804",
                              dir_id = 0)

# Run function: get service ID by day in date range
study_service_ids <- get_gtfs_service_dates(gtfs = lineE_gtfs,
                                            date_min = study_date,
                                            date_max = study_date,
                                            use_calendar_table = "calendar")
print(study_service_ids)
}
