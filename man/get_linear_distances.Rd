% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avl_cleaning.R
\name{get_linear_distances}
\alias{get_linear_distances}
\title{Linearize latitude-longitude GPS points on a provided route shape}
\usage{
get_linear_distances(
  avl_df,
  shape_geometry,
  clip_buffer = NULL,
  original_crs = 4326,
  project_crs = 4326
)
}
\arguments{
\item{avl_df}{A dataframe of raw AVL data. Must include at least \code{longitude}
and \code{latitude} columns. See \code{validate_tides()}.}

\item{shape_geometry}{The SF object to project onto. Must be only one shape.
See \code{get_shape_geometry()}.}

\item{clip_buffer}{Optional. The distance, in units of the chosen spatial
projection, to clip the GPS points. Only points within this distance of the
\code{shape_geometry} will be kept. Default is \code{NULL}, where no clip will be
applied.}

\item{original_crs}{Optional. A numeric EPSG identifier. If a dataframe is
provided for \code{points}, this will be used to define the coordinate system of
the longitude / latitude values. Default is 4326 (WGS 84 ellipsoid).}

\item{project_crs}{Optional. A numeric EPSG identifer indicating the
coordinate system to use for spatial calculations. Consider setting to a
Euclidian projection, such as the appropriate UTM zone. Default is 4326 (WGS
84 ellipsoid).}
}
\value{
The input \code{avl_df} with \code{latitude} and \code{longitude} columns replaced
by a \code{distance} column, in the units of the spatial projection used (e.g.,
meters if using WGS UTM).
}
\description{
This functions projects raw AVL data, as GPS latitude-longitude points, onto
a provided route geometry, returning each point's distance of that point
along the shape its the beginning terminal.
}
\details{
To simplify the user experience, this function takes in \code{shape_geometry}
as an \code{sf} object. The internal calculations, however, are performed using
the \code{geos} library, as it is substantially faster and more memory-efficient
for large datasets.

A limitation of \code{geos}, however, is that it only performs planar, not
ellipsoid, calculations. Given the relatively small spatial range of most
local transit routes, this is reasonable if the data is appropriately
projected into a Euclidean coordinate system. Consider setting
\code{project_crs} to a relevant local plane when performing both
\code{get_linear_distances()} and when retrieving shape geometries
through \code{get_shape_geometry()}.
}
\examples{
# Set my parameters
my_buffer <- 50 # meters
my_crs <- 32611

# Get input data
lineE_avl <- new_transittraj_data("lineE_avl")
lineE_shape <- new_transittraj_data("get_shape_geometry")
dim(lineE_avl)

# Run function
lineE_dists <- get_linear_distances(avl_df = lineE_avl,
                                    shape_geometry = lineE_shape,
                                    clip_buffer = my_buffer,
                                    project_crs = my_crs)
dim(lineE_dists)
head(lineE_dists)
}
